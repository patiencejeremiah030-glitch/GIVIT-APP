# Local dev: relative URL (Vite proxies to Django on port 8001)
VITE_API_URL=/api/v1

# Production (Vercel): use full URL — must end with /api/v1 (not /api/v)
# VITE_API_URL=https://patiencejeremiah.pythonanywhere.com/api/v1

# Google Analytics 4 — optional (leave empty to disable)
# Create a property at https://analytics.google.com/ and copy the Measurement ID (G-XXXXXXXXXX)
VITE_GA_MEASUREMENT_ID=
